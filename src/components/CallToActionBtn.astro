---
import WhatssappIcon from "../assets/whattsappGreenIcon.svg";
---

<div class="buttons-cta">
  <a href="https://www.doctoralia.com.mx/kenia-carrera/medico-general/tijuana?saasonly=true" target="_blank" class="btn-cta primary"> Inscríbete Ahora </a>

  <a href="https://wa.link/u7kc13" class="btn-cta outline" target="_blank">
    <WhatssappIcon class="icon" />
    Envia un mensaje
  </a>
</div>

<style>
  .buttons-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    margin-top: 1rem;
  }

  /* Mobile */
  @media (max-width: 480px) {
    .buttons-cta {
      flex-direction: column;
    }
  }
  .btn-cta.primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;

    padding: 0.9rem 1.6rem;
    border-radius: var(--global-border-radius-sm);

    background: linear-gradient(135deg, #4f46e5, #6366f1);
    color: #fff;

    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;

    box-shadow: 0 10px 24px rgba(79, 70, 229, 0.35);
    transition:
      transform 0.2s ease,
      box-shadow 0.2s ease,
      background 0.2s ease;
  }

  .btn-cta.primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 14px 32px rgba(79, 70, 229, 0.45);
  }

  .btn-cta.primary:active {
    transform: translateY(0);
    box-shadow: 0 6px 14px rgba(79, 70, 229, 0.3);
  }

  .btn-cta.outline {
    display: inline-flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;

    padding: 0.8rem 1rem;
    border-radius: var(--global-border-radius-sm);

    background-color: transparent;
    border: 2px solid #25d366;
    color: #25d366;

    font-weight: 600;
    font-size: 0.95rem;
    text-decoration: none;

    transition:
      background-color 0.2s ease,
      color 0.2s ease,
      transform 0.15s ease,
      box-shadow 0.15s ease;
  }

  .btn-cta.outline .icon {
    width: 22px;
    height: 22px;
    fill: currentColor;
  }

  /* Hover */
  .btn-cta.outline:hover {
    background-color: #25d366;
    color: #fff;
    transform: translateY(-1px);
    box-shadow: 0 8px 20px rgba(37, 211, 102, 0.35);
  }

  /* Active */
  .btn-cta.outline:active {
    transform: translateY(0);
    box-shadow: 0 4px 10px rgba(37, 211, 102, 0.3);
  }

  /* Focus accesible */
  .btn-cta.outline:focus-visible {
    outline: none;
    box-shadow: 0 0 0 3px rgba(37, 211, 102, 0.4);
  }
</style>
